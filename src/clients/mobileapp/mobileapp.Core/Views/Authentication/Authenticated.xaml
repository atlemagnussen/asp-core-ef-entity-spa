<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="mobileapp.Core.Views.Authentication.Authenticated">

    <ContentView.Resources>
        <ResourceDictionary>
            <Color x:Key="Primary">#2196F3</Color>
            <Color x:Key="Accent">#96d1ff</Color>
            <Color x:Key="LightTextColor">#999999</Color>
        </ResourceDictionary>
    </ContentView.Resources>
    
    <ContentView.Content>
        <StackLayout>
            <Label Text="Is Authenticated" />
            <Button Margin="0,10,0,0" Text="Log out"
                        Command="{Binding LogoutCommand}"
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="White" />
            <Label Text="Username::" />
            <Label Text="{Binding UserName}" />
            <Label Text="Access token::" />
            <Label Text="{Binding AccessToken}" />
            <Label Text="Identity token::" />
            <Label Text="{Binding IdToken}" />
            <Label Text="Refresh token::" />
            <Label Text="{Binding Refresh token}" />
        </StackLayout>
    </ContentView.Content>
</ContentView>